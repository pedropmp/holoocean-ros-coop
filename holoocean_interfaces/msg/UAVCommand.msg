# UCommand message
# Contains header and U_control command

std_msgs/Header header

# floating point vector used to specify the pitch, roll, and yaw targets
float64[3] orientation

# altitude targets
int64 altitude